import React from 'react'
import { OwnerVoiceEnrollment } from '../voice/OwnerVoiceEnrollment'

/** Settings wrapper around the shared owner voice enrollment surface. */
export const VoiceEnrollmentCard: React.FC = () => (
  <OwnerVoiceEnrollment variant="settings" />
)
